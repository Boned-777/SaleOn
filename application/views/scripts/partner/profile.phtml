<?php global $translate; ?>
<br />
<div id="view-content">
    <div class="row">
        <div class="span6">
            <div class="legend"><?php echo $translate->getAdapter()->translate("set_new_password"); ?></div>
            <?php echo $this->changePasswordForm->setAction($this->url()); ?>
            <div class="legend"><?php echo $translate->getAdapter()->translate("info"); ?></div>
            <div class="gray"><?php echo $translate->getAdapter()->translate("login"); ?> </div>
            <span><?php echo $this->email; ?></span>
            <br><br>
            <div class="gray"><?php echo $translate->getAdapter()->translate("tarifs"); ?></div>
            <table class="table table-bordered" style="display: none;">
              <thead>
                <tr>
                  <th><?php echo $translate->getAdapter()->translate("geo_name"); ?></th>
                  <th><?php echo $translate->getAdapter()->translate("day_price"); ?></th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td><?php echo $translate->getAdapter()->translate("all_ukraine"); ?></td>
                  <td>0 <?php echo $translate->getAdapter()->translate("uah"); ?></td>
                </tr>
                <tr>
                  <td><?php echo $translate->getAdapter()->translate("all_region"); ?></td>
                  <td>0 <?php echo $translate->getAdapter()->translate("uah"); ?></td>
                </tr>
                <tr>
                  <td><?php echo $translate->getAdapter()->translate("district"); ?></td>
                  <td>0 <?php echo $translate->getAdapter()->translate("uah"); ?></td>
                </tr>
              </tbody>
            </table>
        </div>
        <div class="span6">
            <div class="legend"><?php echo $translate->getAdapter()->translate("profile"); ?></div>
            <?php echo $this->form->setAction($this->url()); ?>
        </div>
    </div>
</div>

<script src="http://maps.googleapis.com/maps/api/js?sensor=false&amp;libraries=places"></script>
<script type="text/javascript" src="/js/jquery/jquery.geocomplete.min.js"></script>
<script type="text/javascript" src="/js/jquery/jquery.maskedinput.min.js"></script>
<script type="text/javascript">
    $(function () {
        $("#phone").add("#phone1").add("#phone2").mask("(999) 999-9999");
        $("#address").geocomplete();

        var clearAutocompleter = function (select, hiddenInput) {
          select.val("");
          hiddenInput.val("0");    
        }
        var brandName = $("#brand_name");
        brandName.click(function() {
            clearAutocompleter(brandName, $("#brand"));
        });
        brandName.focus(function() {
            clearAutocompleter(brandName, $("#brand"));
        });

    });
</script>
